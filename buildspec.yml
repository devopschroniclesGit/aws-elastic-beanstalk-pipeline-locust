version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - echo Installing dependencies
      - pip install -r requirements.txt

  build:
    commands:
      - echo Running build state
      - python -m py_compile app.py

  artifacts:
    files:
      - '**/*'
